/**
 * this is the state for contact
 * @property {String} id the reference id in the config.json
 * @property {Boolean} active the visibility of the modal window
 * @property {Boolean} renderToWindow should this be a standard modal window opened on the left side of the screen
 */
export default {
    id: "contact",
    active: false,
    renderToWindow: true
};
