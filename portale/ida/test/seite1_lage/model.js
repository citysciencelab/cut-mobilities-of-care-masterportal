define([
    "backbone"
], function (Backbone) {

    var Seite1LageModel = Backbone.Model.extend({
        defaults: {
        },
        initialize: function () {
        }
    });

    return new Seite1LageModel();
});
