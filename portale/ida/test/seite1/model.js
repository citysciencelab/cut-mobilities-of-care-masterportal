define([
    "backbone"
], function (Backbone) {

    var Seite1Model = Backbone.Model.extend({
        defaults: {
        },
        initialize: function () {
        }
    });

    return new Seite1Model();
});
