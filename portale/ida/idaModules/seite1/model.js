define([
    "backbone"
], function (Backbone) {
    "use strict";
    var Seite1Model = Backbone.Model.extend({
        defaults: {
        },
        initialize: function () {
        }
    });

    return new Seite1Model();
});
